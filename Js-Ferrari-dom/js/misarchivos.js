

const week = document.getElementById("week")
    console.log(week.innerHTML);

    const weekBonus = document.createElement("li")
    weekBonus.innerText = "SEMANAL"

    week.append(weekBonus)
    